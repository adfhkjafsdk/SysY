int main() {
	/** ***************
	 * 	红红火火恍恍惚惚*/ 
	// return 0;
	// return +0x3f;
	// return 10 && 6;
//   return /* OASFD */ +(- -!6);  // 看起来像个颜文字
	// return ! (20 * -90 + 30 * 5 * 6 > 100 && 1+1+1!=2) * (2-1) || 8*/*eight*/8*/*huit*/8!=01000;
	// return (1+2*3+5)%40 + !!!!!0;
	// const int N = 50 * 2;
	const int N = 50*2, M = 100*100+1;
	// return N + 30 + !N;
	int hello = N+30;
	hello = hello + 100 + 2*M;
	int tmp, qwerty = hello * hello, tmp_2;
	// return 0;
	// return hello;
	return hello == 20232 && qwerty/hello == hello && hello/1000 >= 19;
	// return N+M;
}